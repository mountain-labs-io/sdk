# @mountain-labs/sdk

TypeScript SDK for Mountain Labs CMS.

## Installation

```bash
npm install @mountain-labs/sdk
```

## Usage

```ts
import { createClient } from '@mountain-labs/sdk';

const client = createClient({ token: 'your-api-token' });
```
